# Agent 记忆与 RAG

短期记忆通常指当前对话中的 messages，它只在程序运行期间存在。持久化记忆则会把 messages 保存到 JSON 文件或数据库，程序重新启动后可以恢复。

聊天记录记忆和知识库检索不是一回事。聊天记录保存发生过什么，RAG 则从大量外部文档中检索与当前问题相关的知识片段。

RAG 的基本流程是：加载文档，切分文本，使用 Embedding 模型把问题和文档片段转换成向量，检索语义最接近的片段，把片段放入模型上下文，然后生成基于资料的回答。

