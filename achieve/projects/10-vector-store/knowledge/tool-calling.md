# 工具调用

工具调用是让模型连接外部能力的一种方式。模型不会直接执行 Python 函数，而是根据工具名称、描述和参数 Schema 提出调用请求。

程序收到请求后，负责校验参数、执行真正的函数，并把执行结果作为 tool 消息发回模型。模型再根据工具结果生成最终回答或继续调用其他工具。

工具调用通常包含工具名称、调用 ID 和 JSON 格式的参数。调用 ID 用于把工具执行结果和原始调用对应起来。

